numpy>=1.24
sentence-transformers>=2.2.2

# FAISS is optional and provided in separate files:
# - CPU: requirements-faiss-cpu.txt
# - GPU: requirements-faiss-gpu.txt (typically installed via conda, see RUN.md)
