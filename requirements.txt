numpy>=1.24
sentence-transformers>=2.2.2

# FAISS Python wheels are generally not available on Windows via pip.
# - Linux/macOS: `pip install -r requirements.txt` will install faiss-cpu.
# - Windows: install FAISS via conda, see RUN.md.
faiss-cpu>=1.7.4; platform_system != "Windows"
